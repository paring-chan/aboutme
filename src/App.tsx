import React from 'react'
import MainPage from './views/Main'

interface AppProps {}

function App({}: AppProps) {
    return (
        <div>
            <MainPage />
        </div>
    )
}

export default App
